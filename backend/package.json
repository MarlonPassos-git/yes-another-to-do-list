{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend for the project yes-another-to-do-list",
  "main": "",
  "scripts": {
    "test": "jest --passWithNoTests --silent --runInBand",
    "test:unit": "jest --passWithNoTests --silent --runInBand --testMatch=\"**/*.spec.ts\"",
    "test:cov": "jest --passWithNoTests --silent --runInBand --coverage",
    "dev": "tsx --watch ./src/server.ts",
    "build": "rm -rf dist && tsc"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "eslint": "^8.36.0",
    "jest": "^29.5.0",
    "pretier": "^0.0.1",
    "ts-jest": "^29.0.5",
    "tsx": "^3.12.6",
    "typescript": "^5.0.2"
  }
}